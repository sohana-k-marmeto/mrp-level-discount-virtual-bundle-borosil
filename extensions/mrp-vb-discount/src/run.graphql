query RunInput {
  cart {
    lines {
      id
      quantity
      discountApplicable: attribute(key: "_discountApplicable") {
        value
      }
      discountAmount:attribute(key: "_discountAmount") {
        value
      }
    }
  }
}
